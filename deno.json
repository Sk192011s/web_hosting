{
  "tasks": {
    "start": "deno run --allow-net --allow-read --allow-env --unstable-kv src/main.tsx"
  },
  "cron": {
    "cleanup": {
      "schedule": "0 0 * * *", 
      "command": "curl https://your-project-name.deno.dev/api/cron/cleanup"
    }
  }
}
